This directory should contain textures, audio, etc. that you would like to load in using the ContentsManager.

Build tasks will manage copying the contents over to the destination so do not add these files to the project directly.